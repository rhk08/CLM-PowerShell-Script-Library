# Test Folder Breakdown

tests/input/docx/      # Word documents used as test inputs
tests/input/xml/       # Raw XML test files
tests/input/invalid/   # Broken or invalid test cases
tests/expected/        # Known-good expected outputs
tests/temp/            # Disposable files created during tests
